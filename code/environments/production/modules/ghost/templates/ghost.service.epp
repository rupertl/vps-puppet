[Unit]
Description=Ghost blog
After=network.target

[Service]
Type=simple
PIDFile=/run/ghost.pid
WorkingDirectory=<%= $ghost::dir %>
User=<%= $ghost::user %>
Group=<%= $ghost::user %>
ExecStart=<%= $ghost::bin %>/start.sh
StandardOutput=null

[Install]
WantedBy=multi-user.target